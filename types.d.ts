import { IconType } from 'react-icons';

export interface Menu {
  id: number;
  name: string;
  url: string;
}

export interface Socials {
  id:number,
  icon:IconType,
  name:string,
  url:string
}
